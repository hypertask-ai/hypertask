# E2E verification fleet

> Hand-maintained page (not auto-generated by OpenWiki — it documents a harness that lives outside this repo). Do not delete on refresh.

Status as of 2026-08-07. Tracking ticket: https://app.hypertask.ai/detail/project-15/4985

LLM agents drive a real browser through user flows against production and judge the result from screenshots. Each flow runs in its own E2B cloud sandbox. It exists because nobody manually QAs the app after every merge.

## Where it lives

- Repo: `~/projects/hypertask-verify` (private GitHub `hypertask-ai/hypertask-verify`), branch `app-suite` in worktree `~/projects/hypertask-verify-app`. This is not one of the organization's public repositories.
- Dashboard: https://hypertask.app/verify (page source in `hypertask-analytics`, `src/pages/verify.astro`; reads `explorations/verify/latest.json` + `index.json`).
- Board identity: Hypertask agent **Verification Fleet** (`89c7e230-ee96-4699-aba9-3024360986e2`). Its tickets carry the `e2b` label and appear in the "Verification Fleet (e2b)" view on project 15.

## Two suites

| Suite | Command | Flows | Surface |
|---|---|---|---|
| `app` (priority) | `node orchestrator.mjs --suite app` | 25 | Logged-in production app |
| `demo` | `node orchestrator.mjs --suite demo` | 50 | Anonymous `/demo` guest boards |

The demo suite came first but `/demo` is the trial surface, not the product; the logged-in suite is what matters (Valentin, 2026-08-06).

## How a flow runs

1. Orchestrator creates a throwaway board `verify-<runId>-<flow>` owned by the test account and seeds it through the API.
2. It spawns an E2B sandbox (**`desktop` template** — the base template crashes Chromium) and injects session cookies.
3. `openai/gpt-5.6-luna` at `xhigh` drives Playwright step by step from screenshots + a DOM summary, max 25 steps.
4. A judge model reviews screenshots and the action log and issues pass/fail.
5. Cleanup deletes the board in a `finally`. A hard kill skips it, leaving stray boards — `findThrowawayBoards`/`cleanupThrowawayBoards` in `app-fixtures.mjs` clean up.

## Auth (logged-in suite)

Dedicated production account `verification-fleet@crolab.org`, **userId 1248**. Never Valentin's account.

Login is headless: the orchestrator pulls `JWT_SECRET`/`JWT_ISSUER`/`JWT_AUDIENCE` from the Vercel API (project `hypertasks-prod`) at runtime, mints a short-lived `email-link` JWT, POSTs it to `/api/auth/verify-email-token`, and injects only the resulting `nookies_user` + `ht_session` cookies into the sandbox browser.

**The signing secret never enters a sandbox or a tracked file.** Runtime tripwires enforce this; keep it that way.

## Constraints that bite

- **Free plan = 3 boards.** Each in-flight flow holds one throwaway board, so app-suite concurrency is capped at 3 (`APP_SUITE_CONCURRENCY` in `config.mjs`). Higher concurrency returns `HTTP 403: Free plan includes 3 boards`. The account's default `MyBoard` was deleted to free the third slot; the app does not recreate it.
- **Hypertask is keyboard-first.** Most actions (rename board, add column, archive, filters) exist only in the Ctrl+K command center, not in settings pages. Agents that hunt for buttons burn all 25 steps and fail. Flow prompts must name the command-center path.
- **DeepSeek v4 flash cannot be used.** It is text-only on OpenRouter; every call returns `404 No endpoints found that support image input`. Any driver/judge swap must be vision-capable.

## Cost and billing

All LLM calls bill to a **dedicated OpenRouter key** for this project (`~/.config/openrouter/credentials.env`), visible per run at https://openrouter.ai/activity. E2B sandbox compute runs on the Hobby tier's one-time $100 credits (`~/.config/e2b/credentials.env`).

- App suite (25 flows): **~$0.48**
- Demo suite (50 flows): **~$0.96**

An earlier $10.19 figure was wrong: the price table had Luna at 10x its real cost.

## Results so far

- Demo suite: 34/50 pass.
- App suite: 15/25 pass, **but** deterministic triage (`triage.mjs`, no LLM) showed 9 of the 10 failures were the agent driving badly, not product defects. Real pass rate for the product is higher than the raw number.

Bugs it found, all confirmed by hand before filing:

- https://app.hypertask.ai/detail/project-15/4989 — demo-board search returned nothing. **Fixed, closed.**
- https://app.hypertask.ai/detail/project-15/5029 — guests cannot edit task descriptions (`contenteditable=false`). Open.
- https://app.hypertask.ai/detail/project-15/5167 — archiving from task detail has no working undo. Open.

## Rules for anyone running this

- **Triage before filing.** LLM verdicts are not evidence. Re-run the failing action deterministically (see `triage.mjs`) and confirm at the data level. Several confident-looking failures turned out to be the agent's fault, a stale API read, or a cookie banner swallowing a keypress.
- **No cron yet.** Nightly scheduling waits until the harness fails only for real reasons; a daily report of 10 false reds trains everyone to ignore it.
- **Never point the fleet at a real user's account or board.** Flows must stay on their own throwaway board.

## TestSprite

Status as of 2026-09-16. TestSprite provides a small production API contract suite and logged-out browser smoke suite. Tests must not use Valentin's account or mutate any board except the dedicated **TestSprite QA** board owned by QA userId 985.

| Project | ID | Tests | Dashboard |
|---|---|---:|---|
| `Hypertask MCP API` | `64648e88-03a4-4355-9e14-dc28f4e8cc7e` | 14 backend contracts | [TestSprite](https://www.testsprite.com/dashboard-v3/o/ed490fba-6881-5950-a238-4bc8561b3b57/projects/64648e88-03a4-4355-9e14-dc28f4e8cc7e) |
| `Hypertask Web` | `411d64f3-c9ec-4032-a4e5-ad0e3f6f57d9` | 4 logged-out browser checks | [TestSprite](https://www.testsprite.com/dashboard-v3/o/ed490fba-6881-5950-a238-4bc8561b3b57/projects/411d64f3-c9ec-4032-a4e5-ad0e3f6f57d9) |

The backend suite covers MCP identity/context, isolated-board provisioning, projects and sections, task create/read/update/move/search/archive, labels, and HTML comment create/list. The label-filter contract guards the regression tracked at https://app.hypertask.ai/detail/project-15/6475. The frontend suite covers `/login`, the logged-out `/pricing` redirect, `/developers`, and a public 404. Its project URL is deliberately `/login`: booting a browser at `/` provisions a guest demo board and violates the suite's isolation rule.

Credentials are local only. Source `/home/valentin/.config/testsprite/credentials.env` for the TestSprite key. The backend project's Bearer credential is the account-scoped MCP token for QA userId 985, sourced from `/home/valentin/.config/hypertask-videos/storageState-qa.warmed.json`; rotate the project credential when that QA session token expires. Never copy either value into a test or this repository.

```bash
source /home/valentin/.config/testsprite/credentials.env
testsprite test run --all --project 64648e88-03a4-4355-9e14-dc28f4e8cc7e --wait --timeout 1200
testsprite test run --all --project 411d64f3-c9ec-4032-a4e5-ad0e3f6f57d9 --wait --timeout 1200
```

Backend test source lives in `testsprite/backend/`; add one with `testsprite test create --type backend --name "<contract>" --code-file testsprite/backend/<file>.py --project 64648e88-03a4-4355-9e14-dc28f4e8cc7e`, using only injected `__AUTH_HEADERS__`. Frontend plans live in `testsprite/frontend/`; lint with `testsprite test lint --plan-from-dir testsprite/frontend`, then create a new plan with `testsprite test create --plan-from testsprite/frontend/<file>.json`. A full backend run costs about 2.8 credits and a full frontend run about 2 credits.

`testsprite ci init github` was assessed but not committed. Its generated workflow targets frozen `main`, uses mutable `TestSprite/testsprite-action@v1`, tests configured production rather than the pull-request build, and requires the missing repository secret `TESTSPRITE_API_KEY`; that conflicts with the pinned-action and review-first contract in `docs/ci-policy.yml`.
