services:
  # api:
  #   container_name: hypertasks
  #   build:
  #     context: .
  #     dockerfile: Dockerfile
  #   env_file:
  #     - .env
  #   depends_on:
  #     - db
  #   ports:
  #     - '3000:3000'

  db:
    image: postgres:14.0-alpine
    volumes:
      - postgres_data:/var/lib/postgresql/data/
    env_file:
      - ./.env
    expose:
      - "5678"
    ports:
      - 5678:5678
    command: -p 5678

volumes:
  postgres_data: