/**
 * Internal-only storage slot for Hypertask-managed per-team Vercel AI Gateway
 * keys. It must never be added to the customer BYOK provider allowlist.
 */
export const MANAGED_TEAM_GATEWAY_PROVIDER = "managed_gateway";

