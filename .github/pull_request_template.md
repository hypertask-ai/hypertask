## Outcome

<!-- What user-visible or operational behavior changes? -->

## Ticket

<!-- Full clickable https://app.hypertask.ai/detail/... URL -->

## Components reused

<!-- Required for UI changes. Map each changed exported control to the existing component binding it uses. Reused paths must exist on the base branch. If none fits, use: `ExportedControl` in `src/path/to/control.tsx` -> No existing component fits: `specific justification`. Write "No new controls" when the UI changes without adding a control. -->

- `ExportedControl` in `src/path/to/control.tsx` -> `src/components/reused.tsx`

## Automated test evidence

<!-- Choose the statement that applies and delete the others. -->

- Behavioral change: tests were added or updated in `...`.
- Bug fix: the regression test failed before the fix and passes afterward.
- No new automated test applies because: `...`.

Commands and results:

```text
...
```

## Risk and verification

<!-- Authorization, billing, concurrency, data isolation, rollback, browser or production checks. -->
